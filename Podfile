# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'
pod "HFSwipeView"
pod "PreviewTransition"

target 'WeightList' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
	# pod 'Charts'

end

target 'Swipetest' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

end
